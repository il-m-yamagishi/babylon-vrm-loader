color.rgb = computeMToonColor(
    diffuseBase,
    diffuseColor,
    baseColor.rgb,
    emissiveColor,
    vAmbientColor,
    baseAmbientColor,
    finalSpecular,
    reflectionColor.rgb,
    refractionColor.rgb
);
